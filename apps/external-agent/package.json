{
  "name": "@agentmesh/external-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Independent third-party agent that integrates with AgentMesh using ONLY the public HTTP API. Demonstrates the real integration story and serves as a load-test client.",
  "scripts": {
    "stress:sequential": "tsx src/sequential.ts",
    "stress:parallel": "tsx src/parallel.ts",
    "stress:adversarial": "tsx src/adversarial.ts",
    "stress:all": "tsx src/all.ts",
    "swarm": "tsx src/swarm.ts"
  },
  "dependencies": {
    "viem": "2.21.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
